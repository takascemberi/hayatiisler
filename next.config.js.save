/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  experimental: {
    typedRoutes: true,
    serverActions: true,
    // RAM azaltıcı
    outputFileTracingRoot: false,
  },
  images: {
    unoptimized: true // Netlify’da build RAM patlamasını azaltır
  }
};

module.exports = nextConfig;
